import { MovieVideoResultsList } from './movie-video-results-list';

describe('MovieVideoResultsList', () => {
  it('should create an instance', () => {
    expect(new MovieVideoResultsList()).toBeTruthy();
  });
});
